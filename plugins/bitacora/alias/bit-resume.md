---
description: (alias of /bitacora:resume) Rehydrate a session from a ticket's latest [CTX].
---

Use the `bitacora:session-resume` skill to run the session resume workflow.

A Jira ticket key or a GitHub/GitLab issue number in the arguments below forces the
target ticket; otherwise resolve it from the current branch or recent checkouts.

Arguments: $ARGUMENTS
